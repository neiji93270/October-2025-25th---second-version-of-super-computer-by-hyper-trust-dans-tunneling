//there a re five variables in equations that
//enable async commands on heat cpu behaviour for high energy cpu.
//this is due to large scale ingeneering peoxesses that need both cpu and gpus
//and because this behaviour also need log(10^17) parameters.
